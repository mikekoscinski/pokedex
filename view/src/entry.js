// TODO: This needs to access PSQL data via controller
// The properties should be read here. But the map should be in the entry-list

import React from 'react'

export default function Entry () {
	return (
		<div>
			Bulbasaur
		</div>
	)
};
